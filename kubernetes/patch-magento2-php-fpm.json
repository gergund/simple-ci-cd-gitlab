{
   "spec": {
      "template": {
         "spec": {
            "containers": [
               {
                  "name": "magento2-php-fpm",
                  "image": "{REGISTRY}/magento_php-fpm:{VERSION}"
               }
            ]
         }
      }
   }
}
